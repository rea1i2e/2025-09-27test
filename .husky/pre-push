#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔨 Building project before push..."
npm run build

echo "📦 Adding dist folder to git..."
git add dist/

echo "✅ Build completed and dist folder staged for commit"
